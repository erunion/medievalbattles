<?

@mysql_connect (localhost, user, password);
mysql_select_db(database) or die(deadATconnect);
$dbnam = "database";
$var = @mysql_connect(localhost, user, password) or die(deadATconnect);

?>